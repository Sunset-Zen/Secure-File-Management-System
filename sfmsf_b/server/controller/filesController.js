const data = {
  files: require("../files/files.json"),
  setFiles: function (data) {
    this.files = data;
  },
};

data.files = require("../files/files.json");

// Controller Methods
const AddFile = (req, res) => {
  const file = {};
};
